units metal
boundary p p p
atom_style atomic

neighbor 2.0 bin
neigh_modify every 10 delay 0 check no

read_data _DATA_FILE_

pair_style deepmd _MODELS_LIST_ out_freq _PRINT_FREQ_ out_file _DEVI_OUT_
pair_coeff * *

timestep _TIMESTEP_

dump dcd all dcd _PRINT_FREQ_ _DCD_OUT_

thermo_style custom step time etotal ke pe temp press vol density cella cellb cellc cpuremain
thermo_modify flush yes
thermo _PRINT_FREQ_

velocity all create _TEMPERATURE_ _SEED_VEL_ dist gaussian

fix pl all plumed plumedfile _PLUMED_IN_ outfile _PLUMED_OUT_
fix 1 all nve
fix 2 all temp/csvr _TEMPERATURE_ _TEMPERATURE_ $(100.0*dt) _SEED_THER_
fix 3 all momentum 1 linear 1 1 1

run _NUMBER_OF_STEPS_

write_restart _RESTART_OUT_